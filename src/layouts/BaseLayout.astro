---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  hideCta?: boolean;
}

const siteUrl = 'https://explicit.studio';
const { title = 'Explicit Studio', description = 'A full cycle creative studio creating monopolising brands for technology startups.', ogImage = '/images/open-graph.png', hideCta = false } = Astro.props;
const canonicalUrl = new URL(Astro.url.pathname, siteUrl).href;
const ogImageUrl = new URL(ogImage, siteUrl).href;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta name="author" content="Srijan Mahajan" />
    <meta name="theme-color" content="#141414" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonicalUrl} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>

    <!-- OpenGraph -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:site_name" content="Explicit Studio" />

    <!-- Twitter / X -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
  </head>
  <body>
    <!-- Mobile top bar (hidden on desktop) -->
    <nav class="mobile-topbar">
      <a href="/" class="mobile-topbar__logo"><img src="/images/logos/logo.png" alt="Explicit Studio" class="mobile-topbar__logo-img" /></a>
      <button class="mobile-topbar__hamburger" id="hamburger-btn" aria-label="Menu">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </nav>

    <!-- Two-column layout -->
    <div class="layout">
      <!-- Fixed left sidebar — desktop only -->
      <aside class="sidebar">
        <div class="sidebar__top">
          <nav class="sidebar__nav">
            <a href="/">Home</a>
            <a href="/work">Work</a>
            <a href="/info">Info</a>
            <a href="/contact">Contact</a>
          </nav>
        </div>

        <div class="sidebar__middle">
          <a href="/contact" class="btn-primary sidebar__cta-btn">Get in touch <span class="arrow">&rarr;</span></a>
        </div>

        <div class="sidebar__bottom">
          <p class="sidebar__descriptor">
            A storytelling studio for startups.<br />
            Led by Srijan Mahajan.
          </p>
          <div class="sidebar__logo-large">
            <img src="/images/logos/logo.png" alt="Explicit Studio" class="sidebar__logo-img" />
          </div>
          <div class="sidebar__socials">
            <a href="#" target="_blank" rel="noopener noreferrer">X</a>
            <a href="#" target="_blank" rel="noopener noreferrer">LinkedIn</a>
            <a href="#" target="_blank" rel="noopener noreferrer">Instagram</a>
          </div>
          <a href="mailto:hey@explicit.studio" class="sidebar__email">hey@explicit.studio</a>
        </div>
      </aside>

      <!-- Main scrollable content -->
      <main class="main-content">
        <slot />

        {!hideCta && (
          <div class="site-cta">
            <hr class="site-cta__divider" />
            <h2 class="site-cta__headline">Tell a story that sells.</h2>
            <a href="/contact" class="btn-primary site-cta__button">
              Get in touch <span class="arrow">&rarr;</span>
            </a>
          </div>
        )}
      </main>
    </div>

    <!-- Mobile sidebar overlay (hidden on desktop) -->
    <div class="mobile-sidebar-overlay" id="mobile-overlay"></div>
    <div class="mobile-sidebar" id="mobile-sidebar">
      <button class="mobile-sidebar__close" id="mobile-close" aria-label="Close menu">&times;</button>
      <nav class="mobile-sidebar__nav">
        <a href="/">Home</a>
        <a href="/work">Work</a>
        <a href="/info">Info</a>
        <a href="/contact">Contact</a>
      </nav>
      <div class="mobile-sidebar__bottom">
        <p>A storytelling studio for startups.<br />Led by Srijan Mahajan.</p>
        <div class="mobile-sidebar__socials">
          <a href="#" target="_blank" rel="noopener noreferrer">X</a>
          <a href="#" target="_blank" rel="noopener noreferrer">LinkedIn</a>
          <a href="#" target="_blank" rel="noopener noreferrer">Instagram</a>
        </div>
        <a href="mailto:hey@explicit.studio">hey@explicit.studio</a>
      </div>
    </div>

    <script>
      const hamburger = document.getElementById('hamburger-btn');
      const mobileSidebar = document.getElementById('mobile-sidebar');
      const mobileOverlay = document.getElementById('mobile-overlay');
      const mobileClose = document.getElementById('mobile-close');

      function openMobile() {
        mobileSidebar?.classList.add('open');
        mobileOverlay?.classList.add('open');
        document.body.style.overflow = 'hidden';
      }

      function closeMobile() {
        mobileSidebar?.classList.remove('open');
        mobileOverlay?.classList.remove('open');
        document.body.style.overflow = '';
      }

      hamburger?.addEventListener('click', openMobile);
      mobileOverlay?.addEventListener('click', closeMobile);
      mobileClose?.addEventListener('click', closeMobile);

      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') closeMobile();
      });
    </script>
  </body>
</html>
