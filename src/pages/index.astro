---
import BaseLayout from '../layouts/BaseLayout.astro';
import ProjectCard from '../components/ProjectCard.astro';
import projects from '../data/projects.json';

const workProjects = projects.filter((p) => p.category === 'work');
const explorations = projects.filter((p) => p.category === 'exploration');

const testimonials = [
  {
    quote: "Explicit didn't just give us a brand — they gave us a story that made investors lean in and customers remember our name.",
    name: "Placeholder Name",
    role: "CEO, Startup Co",
    avatar: "",
    cardBg: "#1E1E1E",
    textColor: "#F5F5F5"
  },
  {
    quote: "Most agencies gave us mood boards. Explicit gave us a narrative that became our entire GTM strategy.",
    name: "Placeholder Name",
    role: "Founder, Product Co",
    avatar: "",
    cardBg: "#1E1E1E",
    textColor: "#F5F5F5"
  },
  {
    quote: "We went from 'another AI company' to the one people actually talked about. That's the Explicit difference.",
    name: "Placeholder Name",
    role: "Co-founder, AI Startup",
    avatar: "",
    cardBg: "#1E1E1E",
    textColor: "#F5F5F5"
  },
  {
    quote: "They made biohacking feel like culture, not science. Our community grew itself after that.",
    name: "Placeholder Name",
    role: "Founder, Wellness Brand",
    avatar: "",
    cardBg: "#1E1E1E",
    textColor: "#F5F5F5"
  }
];
---

<BaseLayout title="Explicit Studio — A Storytelling Studio for Startups">

  <!-- Hero -->
  <section class="hero">
    <p class="hero__descriptor">Explicit is a full cycle creative studio creating monopolising brands for technology startups.</p>
    <h1 class="hero__headline">
      Storytelling for founders who believe creativity is a business advantage.
    </h1>
    <div class="hero__media">
      <video src="/images/projects/home-showreel-thumb.mp4" autoplay muted loop playsinline></video>
    </div>
  </section>

  <!-- Logo Bar -->
  <section class="logos-section">
    <p class="logos-section__text">
      Working with ambitious founders & adventure capitalists across AI, Web3 & D2C.
    </p>
    <div class="logos-grid">
      <img src="/images/logos/pause.png" alt="Pause" class="client-logo" />
      <img src="/images/logos/attis.png" alt="Attis" class="client-logo" />
      <img src="/images/logos/QAonC.svg" alt="QAonC" class="client-logo" />
      <img src="/images/logos/qb.png" alt="QB" class="client-logo" />
      <img src="/images/logos/originone.png" alt="Origin One" class="client-logo" />
    </div>
  </section>

  <!-- Selected Work -->
  <section class="work-section reveal">
    <h2 class="work-section__title">Work</h2>
    <div class="home-work-grid">
      {workProjects.map((project) => (
        <ProjectCard project={project} />
      ))}
    </div>
    {explorations.length > 0 && (
      <h3 class="work-section__subtitle">Explorations</h3>
      <div class="home-work-grid">
        {explorations.map((project) => (
          <ProjectCard project={project} />
        ))}
      </div>
    )}
    <a href="/work" class="view-all-card">
      <span class="view-all-card__text">View all work &rarr;</span>
    </a>
  </section>

  <!-- The Problem -->
  <section class="problem-section reveal">
    <div class="problem-section__inner">
      <p class="problem-section__opening">
        If we believe in what you have to say, we'll find the best damn way to say it.
      </p>
      <div class="problem-section__body">
        <p>130,000 startups launch every day. Most die of obscurity.</p>
        <p>Code isn't the differentiator it used to be. And "we're an AI company" isn't a brand.</p>
        <p>The best companies aren't built on great products alone; they're built on stories that make people pay attention before they even see a demo.</p>
        <p>We're a creative studio for founders who've built something real and need the world to know it.</p>
        <p>You've made something the world needs. <strong>Make it Explicit.</strong></p>
      </div>
    </div>
  </section>

  <!-- Testimonials -->
  <section class="testimonials-section reveal">
    <h2 class="testimonials-section__title">We make things people like. We make people like things.</h2>
    <div class="testimonials-grid">
      {testimonials.map(t => (
        <div class="testimonial-card" style={`background-color: ${t.cardBg};`}>
          <p class="testimonial-card__quote" style={`color: ${t.textColor};`}>"{t.quote}"</p>
          <div class="testimonial-card__author">
            <div class="testimonial-card__avatar">
              {t.avatar ? (
                <img src={t.avatar} alt={t.name} />
              ) : (
                <span>{t.name.charAt(0)}</span>
              )}
            </div>
            <div class="testimonial-card__info">
              <span class="testimonial-card__name" style={`color: ${t.textColor};`}>{t.name}</span>
              <span class="testimonial-card__role" style={`color: rgba(255,255,255,0.5);`}>{t.role}</span>
            </div>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- You Belong With Us -->
  <section class="belong-section reveal">
    <h2 class="belong-section__title">You belong with us if you're:</h2>
    <div class="belong-grid">
      <div class="belong-card" style="background-color: #1E1E1E;">
        <h3 class="belong-card__title">Launching a Startup</h3>
        <p class="belong-card__body">You've raised (or are raising) your first round and you need a brand that can attract top talent, build trust with customers, and make investors remember your name. You've got the product — now you need the story.</p>
      </div>
      <div class="belong-card" style="background-color: #1E1E1E;">
        <h3 class="belong-card__title">Launching a Product</h3>
        <p class="belong-card__body">You've scaled past the initial phase and need to refine your narrative for expansion. Whether you're entering new markets or shipping new products, you need strategic storytelling to make it land.</p>
      </div>
      <div class="belong-card" style="background-color: #1E1E1E;">
        <h3 class="belong-card__title">Launching a Fund</h3>
        <p class="belong-card__body">You're building a fund with a unique thesis and need to stand out. From brand positioning to comms strategy, we help you attract the right founders and LPs.</p>
      </div>
    </div>
  </section>

</BaseLayout>

<style>
  /* ===== Hero ===== */
  .hero {
    padding: 40px var(--section-padding-x) 40px;
  }

  .hero__descriptor {
    font-size: var(--text-h3);
    font-weight: 500;
    color: var(--color-text-secondary);
    margin-bottom: 20px;
  }

  .hero__headline {
    font-size: var(--text-hero);
    font-weight: 500;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--color-text);
    max-width: none;
    margin-bottom: 12px;
  }

  .hero__media {
    width: 100%;
    margin-top: 40px;
    border-radius: var(--radius-md);
    overflow: hidden;
  }

  .hero__media video {
    width: 100%;
    display: block;
  }

  /* ===== Logo Bar ===== */
  .logos-section {
    padding: 24px var(--section-padding-x) 24px;
  }

  .logos-section__text {
    font-size: var(--text-h3);
    font-weight: 500;
    color: var(--color-text-secondary);
    margin-bottom: 24px;
  }

  .logos-grid {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    gap: 24px 0;
    width: 100%;
  }

  .client-logo {
    height: 36px;
    width: auto;
  }

  /* ===== Selected Work ===== */
  .work-section {
    padding: 60px var(--section-padding-x) var(--section-padding-y);
  }

  .work-section__title {
    font-size: var(--text-h1);
    font-weight: 500;
    margin-bottom: 48px;
  }

  .home-work-grid {
    display: flex;
    flex-direction: column;
    gap: clamp(24px, 4vw, 40px);
  }

  .work-section__subtitle {
    font-size: var(--text-h2);
    font-weight: 500;
    margin-top: 60px;
    margin-bottom: 48px;
  }

  .view-all-card {
    display: block;
    width: 100%;
    background-color: var(--color-accent);
    border-radius: var(--radius-md);
    padding: clamp(11px, 1.4vw, 20px);
    text-decoration: none;
    color: #FFFFFF;
    text-align: right;
    margin-top: clamp(24px, 3vw, 40px);
  }

  .view-all-card__text {
    font-size: var(--text-h3);
    font-weight: 500;
  }

  /* ===== The Problem ===== */
  .problem-section {
    padding: clamp(40px, 5vw, 60px) var(--section-padding-x);
    background: var(--color-bg);
  }

  .problem-section__inner {
    max-width: 800px;
  }

  .problem-section__opening {
    font-size: var(--text-h2);
    font-weight: 500;
    font-style: normal;
    line-height: 1.3;
    margin-bottom: 32px;
  }

  .problem-section__body p {
    font-size: var(--text-h3);
    font-weight: 400;
    line-height: 1.6;
    margin-bottom: 24px;
    color: var(--color-text);
  }

  .problem-section__body p:last-child {
    margin-bottom: 0;
  }

  .problem-section__body strong {
    font-weight: 700;
  }

  /* ===== Testimonials ===== */
  .testimonials-section {
    padding: clamp(40px, 5vw, 60px) var(--section-padding-x);
    background: var(--color-bg);
  }

  .testimonials-section__title {
    font-size: var(--text-h2);
    font-weight: 500;
    margin-bottom: 32px;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: clamp(16px, 2vw, 24px);
  }

  .testimonial-card {
    border-radius: var(--radius-md);
    padding: clamp(24px, 3vw, 40px);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    min-height: 280px;
  }

  .testimonial-card__quote {
    font-size: var(--text-body);
    font-weight: 400;
    line-height: 1.6;
    margin-bottom: 32px;
  }

  .testimonial-card__author {
    display: flex;
    align-items: center;
    gap: 12px;
  }

  .testimonial-card__avatar {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    overflow: hidden;
    background: rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .testimonial-card__avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .testimonial-card__avatar span {
    font-size: 1rem;
    font-weight: 500;
  }

  .testimonial-card__info {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .testimonial-card__name {
    font-size: var(--text-small);
    font-weight: 500;
  }

  .testimonial-card__role {
    font-size: var(--text-tiny);
    font-weight: 400;
  }

  /* ===== You Belong With Us ===== */
  .belong-section {
    padding: clamp(40px, 5vw, 60px) var(--section-padding-x);
    background: var(--color-bg);
  }

  .belong-section__title {
    font-size: var(--text-h2);
    font-weight: 500;
    margin-bottom: 32px;
  }

  .belong-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: clamp(16px, 2vw, 24px);
  }

  .belong-card {
    border-radius: var(--radius-md);
    padding: clamp(24px, 3vw, 40px);
  }

  .belong-card__title {
    font-size: var(--text-h3);
    font-weight: 700;
    margin-bottom: 16px;
  }

  .belong-card__body {
    font-size: var(--text-body);
    font-weight: 500;
    line-height: 1.7;
    color: var(--color-text-secondary);
  }

  /* ===== Responsive ===== */
  @media (max-width: 767px) {
    .hero {
      padding: 32px var(--section-padding-x) 32px;
    }

    .hero__headline {
      margin-bottom: 8px;
    }

    .hero__media {
      margin-top: 32px;
    }

    .logos-section {
      padding: 32px var(--section-padding-x) 32px;
    }

    .logos-grid {
      justify-content: center;
      gap: 20px 32px;
    }

    .testimonials-grid {
      grid-template-columns: 1fr;
    }

    .belong-grid {
      grid-template-columns: 1fr;
    }

    .client-logo {
      height: 28px;
    }
  }
</style>

<script>
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    },
    { threshold: 0.1 }
  );

  document.querySelectorAll('.reveal').forEach((el) => {
    observer.observe(el);
  });
</script>
